#include<stdio.h>
#include<stdbool.h>

bool ChkAlpha(char ch)
{
    if(ch >= 'A' && ch <= 'Z')
    { return true; }
    else
    { return false; }
}

int main() {
    char cValue,bRet = true;
    printf("Enter a character: ");
    scanf("%c", &cValue);
    
    bRet=ChkAlpha(cValue);
    
    if(bRet == true)
    {  printf("It is Capital character"); }
    else
    {  printf("It is not Capital character"); }
    return 0;
}
